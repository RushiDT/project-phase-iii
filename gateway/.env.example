# ============================================
# Gateway / Base Station Configuration
# ============================================
# Copy to .env and edit with your network values.

# --- Identity ---
GATEWAY_ID=gateway_001

# --- Central Server (Laptop/Cloud IP) ---
# Replace with the IP of the machine running start_system.py
SERVER_HOST=192.168.1.121

# --- Batching ---
# BATCH_SIZE: max logs per batch sent to server
# BATCH_INTERVAL: seconds between batch flushes
BATCH_SIZE=5
BATCH_INTERVAL=10

# --- MQTT Broker ---
# On Pi: use "localhost" (Mosquitto runs locally)
# On PC: use the Pi's LAN IP (e.g. 192.168.1.161)
MQTT_BROKER=localhost
MQTT_USER=admin
MQTT_PASSWORD=password123

# --- Hardware (Raspberry Pi Only) ---
# GPIO pin for alarm buzzer/LED
ALARM_GPIO=17

# --- Pi Health Monitor ---
# Enable background hardware monitoring (CPU temp, RAM, disk)
ENABLE_HEALTH_MONITOR=true
# Temperature threshold (°C) for warning alerts
TEMP_WARNING_THRESHOLD=70

# --- Logging ---
# DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
